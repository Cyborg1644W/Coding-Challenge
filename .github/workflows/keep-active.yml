name: Daily Coding Challenge Metrics Log

on:
  schedule:
    # 03:17 UTC daily ≈ 11:17 AM Philippine time.
    - cron: '17 3 * * *'
  workflow_dispatch: # lets you trigger it manually from the Actions tab to test

permissions:
  contents: write

jobs:
  log-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set git identity
        run: |
          git config user.name "Cyborg1644W"
          git config user.email "reindelandrada@gmail.com"

      - name: Compute per-folder metrics
        run: |
          mkdir -p .activity
          FILE=.activity/metrics.md
          DATE=$(date -u +'%Y-%m-%d')

          count_loc() {
            local
